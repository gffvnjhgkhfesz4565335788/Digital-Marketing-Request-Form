export const metadata = {
  title: 'Digital Marketing Requests',
  description: 'Submit your marketing request',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}